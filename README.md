# Canvas Reaction

A small canvas chain-reaction game ported to a modern Vite project.

## Development

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
npm run preview
```

## Cloudflare Pages

Use the Vite build settings:

- Build command: `npm run build`
- Build output directory: `dist`

